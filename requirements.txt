openai>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pytest>=8.3.0
httpx>=0.27.0
langgraph>=1.1,<2.0
langchain-openai>=1.2,<2.0
langchain-core>=1.3,<2.0
# Pinned exactly (not >=) — an open range here makes pip re-resolve mcp's
# version jointly with traceloop-sdk's whole opentelemetry-instrumentation-*
# matrix from scratch, which backtracks for a very long time. 0.3.2 is the
# version verified locally to resolve instantly against the traceloop-sdk
# pin above (pulls mcp==1.30.0).
langchain-mcp-adapters==0.3.2
requests>=2.32.0
# Pinned to match amp-instrumentation's transitive resolution. tracing.py
# calls Traceloop.init() ourselves after disabling Agent Manager's auto-
# instrumentation (PR wso2/agent-manager#392). 0.51.1 pulls
# opentelemetry-instrumentation-langchain==0.53.3, which is wrapt-1
# compatible. Bump only after openllmetry #4048/#4025 ship and a tagged
# instrumentor release handles wrapt 2.x — see TODOS.md.
traceloop-sdk==0.51.1
# Pin wrapt<2 because opentelemetry-instrumentation-langchain==0.53.3
# (transitively pulled via traceloop-sdk) calls
# wrap_function_wrapper(module=..., name=...) which wrapt 2.x rejected
# (kwarg removed). Without this pin the LangChain instrumentor crashes at
# startup ("Error initializing LangChain instrumentor:
# wrap_function_wrapper() got an unexpected keyword argument 'module'")
# and zero LangGraph spans reach the trace panel — the entire Act 2 demo
# moment silently breaks. Remove this pin only after openllmetry #4048
# and/or #4025 merge AND traceloop-sdk re-pins to a fixed instrumentor
# release. See TODOS.md for the full unblock checklist.
wrapt<2
